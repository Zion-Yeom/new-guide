export declare type LabelValue = {
    label: string;
    value: string;
    parentId: string;
    ordinal: string;
    nodeId: string;
    children: Array<LabelValue>;
};

export declare type LabelValues = Array<LabelValue>;
